import wavesUrl from './layered-waves.svg';
import BackgroundImage from "../BackgroundImage.ts";

const MainBackground = () => <BackgroundImage src={wavesUrl} alt='background'/>

export default MainBackground;
